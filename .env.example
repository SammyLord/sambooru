# A strong, random secret for signing session cookies.
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=your_super_secret_session_key_goes_here

# The host for your Ollama instance.
# This is the default if you are running Ollama on the same machine.
OLLAMA_HOST=http://localhost:11434